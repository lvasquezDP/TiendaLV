import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {PropsTab} from '../../routers/Tab';

export const DInventario = (props: PropsTab<'Ventas'>) => {
  return (
    <View>
      <Text>Detalles</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
