import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {PropsStack} from '../../routers/Auth';

export const HInventario = (props: PropsStack<'HVentas'>) => {
  return (
    <View>
      <Text>Historial</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
